name: cygor

services:
  cygor:
    build: .
    image: cygor:latest

    container_name: cygor
    hostname: cygor

    ports:
      - 8080:8080

    volumes:
      - ${CYGOR_RESULTS_PATH}:/opt/cygor/results

    command: web start --host 0.0.0.0 --port 8080 --load-dir /opt/cygor/results
